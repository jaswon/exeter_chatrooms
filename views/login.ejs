<html>
    <head>
        <title>login</title>
    </head>
    <body>
        <% if (message.length > 0) { %>
            <div><%= message %></div>
        <% } %>
        <form action="/login" method="post">
            <input type="text" name="user" placeholder="Exeter Username">
            <input type="password" name="pass" placeholder="Exeter Password">
            <button type="submit" >Login</button>
        </form>
    </body>
</html>
